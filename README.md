# Machine Learning

This repository contains my machine learning practice files.

## Current Topics

- NumPy arrays
- Array attributes
- Array operations
- Array functions
- Changing data types

## How to Run

Run any Python file from the project folder:

```powershell
python -u "Numpy\numpyarray.py"
```

Install NumPy if it is not already installed:

```powershell
python -m pip install numpy
```
